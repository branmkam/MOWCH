<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mowch">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/circle_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/circle_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DeliveryPopup"
            android:theme="@style/AppTheme.CustomPop2" />
        <activity android:name=".ui.NOTUSED.Notifications" />
        <activity
            android:name=".MapProceedActivity"
            android:theme="@style/AppTheme.CustomPop2" />
        <activity
            android:name=".InstrutionsForRouteActivity"
            android:theme="@style/AppTheme.CustomPop" />
        <activity
            android:name=".NoOnClickDeliveryPopup"
            android:theme="@style/AppTheme.CustomPop2" />
        <activity
            android:name=".NotifyDriverActivity"
            android:theme="@style/AppTheme.CustomPop" />
        <activity
            android:name=".PausePop"
            android:theme="@style/AppTheme.CustomPop2" />
        <activity
            android:name=".RouteInstructionsActivity"
            android:theme="@style/AppTheme.CustomPop" />
        <activity
            android:name=".RoutePopActivity"
            android:theme="@style/AppTheme.CustomPop" />
        <activity
            android:name=".DriversPopActivity"
            android:theme="@style/AppTheme.CustomPop" />
        <activity android:name=".AdminNavigationActivityActivity" />
        <activity android:name=".AgreementsActivity" />
        <activity android:name=".UserNavigationDrawerActivity" />
        <activity
            android:name=".EmergencyPop"
            android:theme="@style/AppTheme.CustomPop" />
        <activity android:name=".ui.NOTUSED.StartScreenActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ForgotPasswordLastActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".LogginginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".APIGetTest" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
    </application>

</manifest>